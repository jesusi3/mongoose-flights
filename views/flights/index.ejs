<%- include("../partials/header")%>

<h1>Flights List</h1>
<table>
    <thead>
        <tr>
            <th>Airline</th>
            <th>Airport </th>
            <th>FlightNo</th>
            <th>Departure</th>
        </tr>
    </thead>
    <tbody>
    <%=  Flight.forEach(function(f) {  %>
        <tr>
            <td><%= f.airline %></td>
            <td><%= f.airport %></td>
            <td><%= f.flghtNo %></td>
            <td><%= f.departs %></td>
        </tr>
    <%= }) %>
    </tbody>
</table>

<%- include("../partials/footer")%>